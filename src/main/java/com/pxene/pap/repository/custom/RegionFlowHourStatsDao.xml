<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pxene.pap.repository.custom.RegionFlowHourStatsDao">
  
  <select id="selectTopN" resultType="java.util.Map">
        SELECT
            t.region_id regionId,
            IFNULL(SUM(t.request_amount),0) AS requestAmount
        FROM
            pap_t_regionflow_hour t
        WHERE
            t.datetime BETWEEN #{beginTime, jdbcType=TIMESTAMP}
        AND #{endTime, jdbcType=TIMESTAMP}
        GROUP BY
            t.region_id
        ORDER BY
            request_amount DESC
        LIMIT #{limitNum, jdbcType=INTEGER}
  </select>
</mapper>